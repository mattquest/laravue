/**
 * Global Scoped Mixins:
 * Any objects you place in this object will be mixed into every
 * Vue component placed within your "components" directory.
 * This gives you the benefit of "global" mixins without over
 * stepping Vue component imported from external libraries.
 */

const mixins = []

export default mixins
